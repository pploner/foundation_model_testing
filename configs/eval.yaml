# @package _global_

defaults:
  - _self_
  - data: collide2v # choose datamodule with `test_dataloader()` for evaluation
  - model: tinyMLP
  - logger: null
  - trainer: default
  - paths: fm_testing
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: /eos/user/p/phploner/foundation_model_testing/logs/train/runs/2025-10-14_16-55-39/checkpoints/last.ckpt
